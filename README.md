# Automotive CAN-Ethernet Gateway ECU

## 1. Overview
This project presents the design and implementation of an **Automotive Gateway ECU** prototype based on the modern **Zonal Architecture** trend. The gateway serves as a central router to exchange data and diagnostic services between a high-speed Ethernet backbone and real-time CAN bus networks.

**Key achievements:**
* Implemented a 3-layered software architecture simulating automotive standards (MCAL, Middleware, Application).
* Developed a routing engine using **FreeRTOS** to handle concurrent network traffic with zero packet loss.
* Successfully translated **UDS (ISO 14229)** diagnostic requests from **DoIP** (Ethernet) to **DoCAN** and vice versa.

## 2. System Architecture

### Software Architecture
The embedded software is strictly structured into 3 layers to ensure modularity and hardware abstraction:
1. **Application Layer:** Contains the `Gateway Core Task` (High Priority) for routing decisions and `UDS Diagnostic Service` handling.
2. **Middleware Layer:** Integrates **FreeRTOS** for task scheduling/IPC (Queues, Event Flags) and **LwIP Stack** for TCP/UDP communication.
3. **Driver Layer (MCAL):** Hardware abstraction using STM32 HAL for Ethernet MAC, CAN Controller, and Timers.

![Software Architecture Diagram](images/software_arch.png)

### Hardware Setup
* **Gateway MCU:** STM32F407 Discovery (ARM Cortex-M4)
* **Ethernet Interface:** LAN8720 PHY via RMII
* **CAN Interface:** TJA1050 Transceiver (CAN 2.0B)
* **CAN Nodes (Simulated ECUs):** STM32F103 ("Blue Pill") used as sensor nodes, actuators, and traffic generators.

![Gateway 3D PCB](images/pcb_3d.png)

## 3. Repository Structure

```text
Automotive-Gateway-Thesis/
├── Gateway_ECU/              # Core routing firmware for STM32F407 (FreeRTOS + LwIP)
├── NodeA_Sensor/             # Firmware for STM32F103 (Ultrasonic sensor data acquisition)
├── NodeB_Actuator/           # Firmware for STM32F103 (UDS 0x2F IO Control actuator)
├── NodeC_TrafficGenerator/   # Firmware for STM32F103 (CAN flood testing)
├── PC_Tester_GUI/            # Python CustomTkinter Dashboard & CSV logs
│   ├── gateway_dashboard.py
│   └── requirements.txt
├── Result_Stress_Test/       # Wireshark captures and latency evaluation data
├── images/                   # Diagrams and hardware photos for documentation
└── README.md
```

## 4. Core Functionalities

* **CAN Protocol (ISO 11898):** Support for Standard (11-bit) IDs with Hardware Acceptance Filtering to reduce CPU overhead.
* **Message Routing:** Bi-directional data transfer between CAN and UDP/TCP sockets.
* **UDS Diagnostic (ISO 14229):** 
  * Translates DoIP requests (e.g., `Read Data By Identifier 0x22`, `ECU Reset 0x11`) from a PC Client into DoCAN frames.
  * Routes the frame to the specific target ECU on the CAN bus.
  * Relays the ECU's response back to the PC Client via Ethernet.
* **Real-time Monitoring:** Python-based GUI for data plotting and automated CSV logging.

## 5. Development Tools & Execution
* **IDE:** Keil uVision (MDK-ARM) / STM32CubeMX
* **Language:** Embedded C (Firmware), Python 3 (GUI)
* **Testing & Validation:** Wireshark, Hercules SETUP utility, Custom multi-node CAN testbench.

**To run the PC Dashboard:**
```bash
cd PC_Tester_GUI
pip install -r requirements.txt
python gateway_dashboard.py
```

## 6. Performance Evaluation
* Evaluated system latency (Round-Trip Time - RTT) for various UDS requests (Minimum latency achieved: ~0.35 ms for Cache Hits).
* Measured maximum throughput (frames per second) and verified system stability under high network load generated by a dedicated CAN node.
* Detailed testing logs and PCAP files can be found in the `Result_Stress_Test` directory.